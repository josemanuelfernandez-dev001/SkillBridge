apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "processor.configmapName" . }}
  labels:
    {{- include "processor.labels" . | nindent 4 }}
data:
{{- if .Values.env }}
{{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
{{- else }}
  {}
{{- end }}
